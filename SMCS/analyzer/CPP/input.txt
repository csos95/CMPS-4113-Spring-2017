#include <iostream>
#include <stuff.h>

//this is the main function
int main(void) {
    int x = 2;
    float y = 3.5;
    float z = x * y;
    int w = x + x;
    int u = w - x;
    printf("z: %d\n", z);
    return 0;
}
